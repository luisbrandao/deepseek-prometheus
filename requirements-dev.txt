# Test-only dependencies. Deliberately NOT in requirements.txt — the runtime
# image installs that one and has no reason to carry a test runner.
#   pip install -r requirements.txt -r requirements-dev.txt && pytest
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
