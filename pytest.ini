[pytest]
testpaths = tests
asyncio_mode = auto
# Every test drives the async functions directly; there is no event loop to
# share between them, so each test gets a fresh one.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning
